# Changelog

## 1.1.0

- First public release (work in progress).
- Separate wave scales on land/shallow water (`LandWaveScale`) and open sea (`SeaWaveScale`), blended by water depth (`OpenSeaDepth`). Scales range 0–5.
